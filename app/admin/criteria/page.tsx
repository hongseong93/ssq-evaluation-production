"use client";
import { useEffect,useState } from "react";import { Plus } from "lucide-react";import { AdminShell,Button,Card,Field,TextArea,TextInput } from "@/components/ui";
export default function CriteriaPage(){const [items,setItems]=useState<any[]>([]),[message,setMessage]=useState(""),[form,setForm]=useState({division:"template",title:"",maxScore:"",description:"",questions:""});const load=async()=>{const r=await fetch("/api/admin/criteria");const d=await r.json();setItems(d.criteria||[])};useEffect(()=>{void load()},[]);const save=async()=>{const r=await fetch("/api/admin/criteria",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(form)});const d=await r.json();setMessage(r.ok?"평가 항목이 추가되었습니다.":d.message);if(r.ok){setForm({division:"template",title:"",maxScore:"",description:"",questions:""});void load()}};return <AdminShell title="평가 항목 관리"><div className="grid gap-5 xl:grid-cols-[1fr_360px]"><Card>{items.map(i=><div key={i.id} className="grid grid-cols-4 gap-3 border-b p-4 text-sm"><span>{i.display_order??i.order}</span><span>{i.division}</span><span>{i.title}</span><span>{i.max_score??i.maxScore}점</span></div>)}</Card><Card className="p-5"><h2 className="text-lg font-bold">항목 추가</h2><div className="mt-5 space-y-4"><Field label="부문"><select className="h-10 w-full rounded-md border px-3" value={form.division} onChange={e=>setForm({...form,division:e.target.value})}><option value="template">Template Creation</option><option value="original">Original Creation</option></select></Field><Field label="항목명"><TextInput value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/></Field><Field label="배점"><TextInput type="number" value={form.maxScore} onChange={e=>setForm({...form,maxScore:e.target.value})}/></Field><Field label="설명"><TextArea value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/></Field><Field label="세부 질문"><TextArea value={form.questions} onChange={e=>setForm({...form,questions:e.target.value})}/></Field><Button className="w-full" onClick={save}><Plus size={16}/> 평가 항목 추가</Button>{message&&<p>{message}</p>}</div></Card></div></AdminShell>}
